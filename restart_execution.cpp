// A separate file to execute_task.cpp containing a main function. Reads from a
// savefile the time at the start of task execution, delivery zone, number of
// baskets already delivered and the basket composition.
